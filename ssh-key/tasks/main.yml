---
- name: Set authorized key taken from default RSA identity
  become: yes
  authorized_key:
    user: '{{ ssh_user }}'
    state: present
    key: "{{ lookup('file', item) }}"
  with_fileglob:
    - "~/.ssh/*.pub"
